<h3>Adicionar resenha</h3>
<%= simple_form_for [cocktail, review] do |f| %>
<%= f.input :rating, collection: 0..5 %>
<%= f.input :content %>
<%= f.button :submit, 'Add Resenha', class: "btn btn-primary"%>
<% end %>